const { Client } = require('pg'); const client = new Client({ connectionString: process.env.DATABASE_URL, ssl: { rejectUnauthorized: false } }); async function checkAdmins() { try { await client.connect(); const result = await client.query('SELECT username, role, created_at FROM admins ORDER BY created_at'); console.log('Production admins:', result.rows); } catch (error) { console.error('Error:', error.message); } finally { await client.end(); } } checkAdmins();
